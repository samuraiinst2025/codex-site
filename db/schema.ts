import { integer, sqliteTable, text } from 'drizzle-orm/sqlite-core';
export const cards = sqliteTable('cards', { id: integer('id').primaryKey({ autoIncrement: true }), userId: text('user_id').notNull(), japanese: text('japanese').notNull(), english: text('english').notNull(), example: text('example'), note: text('note'), nextReviewAt: integer('next_review_at', { mode: 'timestamp' }).notNull(), createdAt: integer('created_at', { mode: 'timestamp' }).notNull() });
export const reviewLogs = sqliteTable('review_logs', { id: integer('id').primaryKey({ autoIncrement: true }), cardId: integer('card_id').notNull(), direction: text('direction').notNull(), result: integer('result', { mode: 'boolean' }).notNull(), reviewedAt: integer('reviewed_at', { mode: 'timestamp' }).notNull() });
